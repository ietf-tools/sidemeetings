# Side Meetings

## Setup

Requirements: **Node.js 26.x**

1. Create a copy of `.env.sample` to `.env` and set the values.
2. Install npm dependencies:
   ```sh
   npm install
   ```

### Run in dev mode

```sh
npm run dev
```

### Build for production

```sh
npm run build
```
