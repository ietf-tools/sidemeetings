# Dependencies — reinstalled per-arch inside the image
node_modules

# Build artifacts — regenerated by the builder stage
.output
.nuxt
.nitro
.cache
.data
dist

# Secrets / local env — never bake into the image
.env
.env.*

# VCS & CI
.git
.github
.gitignore
.gitattributes

# Local tooling & non-app dirs
.devcontainer
.vscode
docker
old
project
docker-compose.yml

# Logs
*.log
